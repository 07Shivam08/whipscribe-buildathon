# Gemini CLI — WhipScribe Buildathon

Read `AGENTS.md` first; it is the project brief and the rules. In short: own
account only, no secrets in commits, real API responses over guesses, small
PRs, and say what is unfinished. Track 2 work lives in `apps/<your-name>/`.
